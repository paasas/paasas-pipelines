---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: harbor.paasas.io/public/ci
    tag: latest

inputs:
- name: ci-src
- name: dags-src

params:
  COMPOSER_DAGS_BUCKET_NAME: 
  COMPOSER_DAGS_BUCKET_PATH: 
  COMPOSER_DAGS_PATH:

run:
  path: ci-src/.concourse/tasks/compose-update-dags/compose-update-dags.yaml